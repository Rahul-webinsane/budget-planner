{
    "builds": [
      {
        "src": "path/to/your/source",
        "use": "@vercel/node",
        "config": { "maxLambdaSize": "50mb" },
        "buildCommand": "npm install --force && npm run build"
      }
    ]
  }